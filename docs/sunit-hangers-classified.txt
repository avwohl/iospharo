# SUnit "hangers" — verdicts confirmed on PRISTINE image copies (2026-05-31)
#
# Method: each class alone, fresh image copy, JIT-on then PHARO_NO_JIT=1 in
# sequence, NO concurrent VMs (they share /tmp/sunit_* and corrupt each other).
# The driver's stall-watchdog "hang" flag is unreliable — none of these hang.
#
#   class                    JIT-on       JIT-off      verdict
#   GPointTest               18P/0F/2E    20P/0F/0E    JIT BUG (testSetX, testPolygonClipping
#                                                       error "receiver ... is nil" only under JIT)
#   GTriangleTest            7P/0F/0E     7P/0F/0E     clean
#   RGMethodDefinitionTest   17P/13F/2E   17P/13F/2E   NOT JIT — fails identically both ways
#                                                       (Ring metamodel / image-compat, not JIT)
#   TestValueWithatHelpTest  completes, 0 concrete tests            not a hang
#   StringTest               clean (fresh-image window 0)           cumulative-state artifact
#
# CONFIRMED JIT BUG: GPointTest — receiver becomes nil under JIT in testSetX
# (setX:setY:) and testPolygonClipping. Next target.
#
# Not yet isolated (still carry the unreliable driver flag — isolate one at a
# time before any verdict): BlockClosuresTestCase, FFICalloutAPITest,
# ClyBrowserToolValidityTest, CodeSimulationTest, ColorTest,
# FileAttributesPluginPrimsTest, MailMessageTest, ManyTestResourceTestCase,
# RBRefactoringChangeTest, RSKernelDensityTest, SimpleTestResourceTestCase,
# SpFontStyleTest, SpLabelPresenterTest, SpMorphicBoxLayoutTest,
# SpPaginatorMorphTest, TFUFFIDerivedTypeMarshallingInCallbackTest,
# WindowsStoreTest, GArcTest, FailingTestResourceTestCase.

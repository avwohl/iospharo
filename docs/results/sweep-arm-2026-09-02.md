# arm64 full SUnit sweep, 2026-09-02

First full sweep on the rebuilt Mac (Command Line Tools only, no Xcode).
`scripts/sunit-sweep.sh` at `STEP=50 PER_BATCH_TIMEOUT=1800`, VM
`build-rel/test_load_image` at `10f5f330`, on the locally prepped
SUnit-runner image (Pharo 13.1 build 745, sha `4f7563d`) with its `.sources`
beside it, machine otherwise idle.  Baseline environment as every recorded
run: `PHARO_CODE_ZONE_MB=192 PHARO_MAX_STEPS=4000000000000
PHARO_MAX_OLD_SPACE_MB=12288`.  Started 16:29, done 18:30.

Raw artifacts: `sweep-arm-2026-09-02/` (sweep log, per-class totals,
non-clean classes, failing selectors).

## Totals

    classes  tests   PASS    FAIL  ERROR  SKIP  TIMEOUT   rate
     2005    27682   27453    21     21    182     5      99.17%   as aggregated
     2007    27692   27461    21     21    182     7      99.17%   + 2 spliced classes

The second line is the first plus two classes the aggregator could not see;
the splice is a harness defect and is fixed, both described below.  **Neither
line is the whole suite**: one batch died and took 39 classes with it, also
below.  Read the number as "2007 of ~2047 classes, 99.17% of the tests that
ran".

For comparison, `sweep-arm-final-2026-08-22.md` (with its two damaged batches
re-run and merged): 2046 classes, 28067 tests, 27727 P, 27 F, 25 E, 182 S,
98.79%.  F+E goes 52 -> 42 on ~375 fewer tests.

## Two classes were dropped by a write splice in the runner — FIXED

`MCSmalltalkhubRepositoryTest` and `TKTWorkerPoolTest` each timed out, and
each lost its `Total:` line into the middle of the watchdog's verdict:

    === MCSmalltalkhubRepositoryTest ===
      RUN: testVersionFromFileNamed
      TIMEOUT: testVersionFromFileNamed (exceeded 80s)
        WD-CLOCK now=Total: 1 P:0 F:0 E:0 S:0 T:1

Every offline aggregator anchors on `^Total:`, so both classes vanished from
the class count and their 10 tests from the totals.  The cause is in
`run_sunit_tests.st`: the P60 watchdog set `testDone := true` **before**
printing its verdict, so the P80 runner main woke out of its poll loop,
wrote the class summary into the half-written line, and then suspended the
watchdog on the "clean up watchdogs" path — which is why the rest of the
`WD-CLOCK` line never appears at all.

Fixed in the `scripts/pharo-headless-test` submodule: the verdict is composed
into a String and written with one `nextPutAll:`, and `testDone` is published
only after the flush.  The kill of the runaway test still happens first, so
the 2026-07-06 orphan race the old ordering existed to prevent stays
prevented.

## One batch died of a Context storm and took 39 classes with it

Batch 1901-1950 (`rc=134`) ran 11 of its 51 classes — `TimespanDoTest`
through `TonelWriterV3Test` — and then exhausted the 12 GB old space:

    [VM] FATAL: old space exhausted during scavenge tenure
    [HEAP-CENSUS] 47437613 objects, 12313 MB total
    [HEAP-CENSUS]  33363299 objs 11725994 KB  Context
    [HEAP-CENSUS]   6656491 objs   364026 KB  Error
    [HEAP-CENSUS]   6664295 objs   312344 KB  FullBlockClosure

Same signature as the storm that killed batch 601-650 on 2026-08-22 and the
same shape as the open "Monticello runaway" item in `docs/WIP.md`.  The ratios
are the useful part: 6.66M `Error`s to 6.66M `FullBlockClosure`s is 1.00, and
33.4M `Context`s to `Error`s is 5.01 — one handler block and about five live
frames per level of an unbounded recursion in which every level signals an
Error and catches it.  Nothing is being freed because the recursion never
unwinds, so every level's frames and its Error stay reachable.

**The missing classes are the trait tests.**  The batch ran indices 1901-1911
(`TimespanDoTest` .. `TonelWriterV3Test`) and died on 1912; batch 1951 starts
at `UndefinedPackageTest`.  Enumerating every class with methods in this
image's `.sources` between those two names gives 41 `*Test`/`*TestCase`
classes, which matches the 39-40 that never ran, and 27 of them are `Trait*`:

    TraitAbstractTest ... TraitWithSlotsTest        (27 classes)
    TrueTest, Tutorial*Test (3), UDPSocket*Test (2),
    UUID*Test (3), Undeclared/Undefined*Test (4)

So the storm starts at or just after the first concrete trait test.  That
matters beyond naming it: `docs/vm-compat-bugs.md` has carried
"`TraitTest` — Cog 54 P / 0 F / 0 E, ours errors, not yet drilled" since
2026-06-01, and today's other newcomer, `OCClassBuilderTest`, also fails on a
**trait composition**.  Re-running 1901-1950 is queued behind the x86_64
sweep, because every SUnit runner invocation reads and writes the same `/tmp`
paths.

`TraitTest` is in that missing range, so its absence from the residual below
is "did not run", not "now clean".

The log also settles LEAD 19 in `docs/vm-compat-bugs.md`: the low-space
circuit breaker meant to stop exactly this printed **nothing**.  It was
sampling the threshold on a bytecode counter while old space drains in
eden-sized (22 MB) tenure steps against a 400 KB threshold, so it could only
fire 1.8% of the time.  Fixed the same day — see `docs/changes.md`.

Batch 1801-1850 shows `rc=124` (hit the 1800 s wall) but reported all 51
classes and the completion marker, i.e. it finished the tests and was killed
during shutdown.  Its results are in the totals.

## Every class with a non-zero F, E or T

    class                                            F   E   T   bucket
    FTTableMorphTest                                 0   1   0   no display
    SpAthensAdapterTest                              0   1   0   no display
    SpComponentListAdapterTest                       0   1   0   no display
    SpListCommonPropertiestTest                      0   5   0   no display
    SpTableCommonPropertiestTest                     0   3   0   no display
    SpTreeAdapterMultipleSelectionTest               0   2   0   no display
    SpTreeTableAdapterMultiColumnMultiSelectionTest  1   1   0   no display
    SpTreeTableAdapterMultiColumnTest                1   1   0   no display
    SpTreeTableAdapterSingleColumnMultiSelectionTest 1   1   0   no display
    SpTreeTableAdapterSingleColumnTest               1   1   0   no display
    StDebuggerActionModelTest                        1   0   0   no display
    StDebuggerInspectorTest                          1   0   0   no display
    StDebuggerTest                                   3   0   0   no display + CI-skipped
    StSpotterModelTest                               2   0   0   no display + CI-skipped
    StSpotterTest                                    1   0   0   no display + CI-skipped
    StTranscriptPresenterTest                        3   0   0   no display
                                                    --  --  --
                                                    15  17   0   16 classes

    ReleaseTest                                      2   0   2   1 defect + 1 prep artifact
    WeakAnnouncerTest                                1   0   0   the same defect; CI-skipped
    LinkInstallerTest                                1   0   0   GC-history, NOT the JIT
    SystemDependenciesTest                           1   0   0   image dependency drift
    OCClassBuilderTest                               0   1   0   image bug, cause named
    RSLinesTest                                      0   2   0   UNTRIAGED
    TKTWorkerTest                                    0   1   1   task-kernel timing
    TKTWorkerPoolTest                                0   0   1   task-kernel timing
    NoUnusedVariablesLeftTest                        0   0   1   activation wall (#6)
    MCSmalltalkhubRepositoryTest                     0   0   1   network
    ZnClientTest                                     1   0   0   testQueryGoogle wants the internet
    ZnHTTPSTest                                      0   0   1   network
                                                    --  --  --
                                                     6   4   7   12 classes

16 of the 28 are the missing display, and
`scripts/pharo-headless-test/setup_fake_gui.st` is measured to clear that
whole bucket (2026-08-22: the same population ran 167 tests 0 F / 0 E with
the prelude filed in).  Four more carry
`skipOnPharoCITestingEnvironment`, so Pharo's own CI does not run them.

## Changed against the 2026-08-22 residual

    now clean          EpFileOutModificationsTest, SpColorPickerTest,
                       SpDatePresenterTest, SpJobListPresenterTest,
                       SpTreeAdapterSingleSelectionTest, SpTreeTableSearchTest,
                       WeakKeyDictionaryTest
    newly non-clean    OCClassBuilderTest, RSLinesTest,
                       NoUnusedVariablesLeftTest, ZnHTTPSTest
    did not run        TraitTest (in the batch that died)

The three timeouts among the newcomers are not new behaviour: 80 s is the
per-test watchdog, and both `ZnHTTPSTest>>testTransfers` and
`MCSmalltalkhubRepositoryTest` reach the public internet.  Every non-network
TIMEOUT in this run belongs to open defect #6, the activation wall —
`NoUnusedVariablesLeftTest>>testNoUnusedTemporaryVariablesLeft` and
`ReleaseTest`'s `testNoShadowedVariablesInMethods` and `testNoNullCharacter`,
all whole-image reflective scans.  `OCClassBuilderTest` is characterised in
`docs/image_issues.md` — the test's trait composition names
`TAsStringCommaAndDelimiterTest`, which build 745 does not define, so the
compiler is right to refuse it.

`RSLinesTest` is the only genuinely untriaged newcomer:

    ERROR: testMarkerOffset          (BlockCannotReturn: )
    ERROR: testMarkersIncludesPoint  (BlockCannotReturn: )

`BlockCannotReturn` is a non-local return from a block whose home context is
dead, which is a VM-visible condition rather than a display one, so this one
is worth reproducing on its own.  It was also in the pre-reboot residual and
clean in the 2026-08-22 run.

## `ReleaseTest>>testUnknownProcesses` changed cause, and the new one is ours

On 2026-08-23 this failed with `MessageNotUnderstood:
DefaultExecutionEnvironment >> #watchDogProcess` and was filed as an image
bug.  In this run it fails differently:

    FAIL: testUnknownProcesses
      Found unknown processes: 'CommandLine handler process':'a Process in
      SessionManager>>snapshot:andQuit:

That is the prep step showing up in its own image.  `12ad7265` moved the
runner prep onto our VM ending in `Smalltalk snapshot: true andQuit: true`,
which is executed *by* the command-line handler process — so the saved image
resumes with that process parked inside `snapshot:andQuit:`, and a test whose
whole job is to enumerate unexpected processes finds it.  A harness artifact
of the current prep, not a VM defect, and the cheapest fix is to snapshot
from a process the handler is not holding.

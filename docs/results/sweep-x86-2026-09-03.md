# x86_64 full SUnit sweep, 2026-09-02 → 03

Same driver, same settings and the same prepped image as
`sweep-arm-2026-09-02.md`: `scripts/sunit-sweep.sh` at `STEP=50
PER_BATCH_TIMEOUT=1800`, `PHARO_CODE_ZONE_MB=192 PHARO_MAX_STEPS=4000000000000
PHARO_MAX_OLD_SPACE_MB=12288`, VM `build-x86/test_load_image` at `10f5f330`.
Started 22:03, done 00:42.  x86_64 runs under Rosetta on this host.

## Totals, and they are the same as arm64's

    arch     classes  tests   PASS    FAIL  ERROR  TIMEOUT  SKIP   rate
    x86_64    2046    28071   27833    22     26      10     180   99.15%
    arm64     2005    27682   27453    21     21       5     182   99.17%

**The x86_64 run is the more complete of the two.**  Batch 1901-1950 — the one
whose Context storm cost arm64 39 classes (defect #23) — ran clean here in
270 s with all 51 classes.  So the storm is arm-only, at least in this pair of
runs, and x86_64 is the first measurement this project has of the trait block.

Batch 1001-1050 was damaged by my own interference mid-sweep (a `test_relaunch`
on the PREPPED image, which auto-starts `SUnitRunner`) and reported
`classes=1`.  It was recovered afterwards in chunks of five —
`recovered_results.txt`, ten chunks, all clean — and is included in the totals
above.

Batch 1801-1850 cost 1800 s here exactly as on arm64: the threaded-FFI
shutdown hang, defect #26, now root-caused and fixed.

## What the trait block says, now that a sweep has run it

Cog runs all 27 `Trait*Test` classes 270 P / 0 F / 0 E in seconds
(`sweep-arm-2026-09-02/cog-trait-baseline.txt`).  We do not:

    TraitTest            53 P / 1 F   testTraitsUsersSanity [Assertion failed]
    TraitFileOutTest      2 P / 2 E   FileDoesNotExistException:
                                        .../Generated-Trait-Test-Package.st
    TraitInTraitClassTest 1 P / 1 T

`TraitFileOutTest` is a working-directory artifact — the test writes its
fileOut into the CWD and the sweep runs from the repo root — so the one to
chase is **`TraitTest>>testTraitsUsersSanity`**, which finally puts a selector
on the entry this repo has carried since 2026-06-01 as "TraitTest — Cog
54 P / 0 E, ours errors, not yet drilled".

## Arch differences, all accounted for

    non-clean only on x86_64                        what it is
    IntegerTest                          T          Rosetta timing (80 s bound)
    SpTreePresenterExpandTest            T          Rosetta timing
    STONWritePrettyPrinterReadTest       T          Rosetta timing
    RBBrowserEnvironmentTest             T          Rosetta timing
    RSSequentialAnimationTest            E          "Error: Time up"
    SpJobListPresenterTest               F          "Got 68 instead of 100"
    ZnStaticFileServerDelegateTest       F          a one-second clock compare
    MCSmalltalkhubRepositoryTest         T          arm has it too, as a TIMEOUT
    TFFunctionCallTest                   E          w64Convention symbol absent
    TFUFFIDifferentCallingConvention...  E          from libTestLibrary.dylib
    TraitFileOutTest                     E          CWD artifact (above)
    TraitInTraitClassTest                T          trait block, arm never ran it
    TraitTest                            F          trait block, arm never ran it

    non-clean only on arm64
    NoUnusedVariablesLeftTest            T          defect #6, activation wall
    StDebuggerActionModelTest            F          timing; clean on x86
    ZnHTTPSTest                          T          network

So **no x86-vs-arm codegen divergence in 28071 tests**.  Every arch-only entry
is a timeout under Rosetta (which runs this build at roughly half arm64's
speed against a fixed 80 s per-test bound), a wall-clock assertion, a missing
Windows-only symbol in our test dylib, a CWD artifact, or a class the other
arch never reached.  The runner now reads `SUNIT_TIMEOUT_MULT`, so the next
x86 sweep should use `SUNIT_TIMEOUT_MULT=2` and the first four disappear.

## The 23 classes non-clean on BOTH

Identical to the arm64 residual and triaged there in full — 14 of them are
defect #24 (the runner must suspend the UI process; reproduced on stock Cog),
three are parity with Cog on the same failing test, and the rest are named
individually.  Two carry over as VM defects:

  * `RSLinesTest` — `BlockCannotReturn`, **on both arches**, which confirms
    defect #22 is arch-independent.  x86 shows one of the two errors
    (`testMarkersIncludesPoint`), arm both.  Root-caused: a trait method copy
    shares the trait's `CompiledBlock`, whose `outerCode` names only the
    trait's method, so the home match by method oop cannot succeed.
  * `TKTWorkerTest` — 4 P / 2 E / 1 T here against arm's 5 P / 1 E / 1 T;
    task-kernel timing either way.

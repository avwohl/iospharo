| terminatee terminator n | terminatee := [[Processor activeProcess suspend] ensure: [nil]] fork. Processor yield. terminator := [terminatee terminate] newProcess. n := 0. [n > 2000] whileFalse: [ | sel | sel := (InstructionStream on: terminator suspendedContext method pc: terminator suspendedContext pc) selectorToSendOrSelf. sel = #handleProcessTerminationOfWaitingContext: ifTrue: [n := 99999] ifFalse: [terminator step. n := n + 1]]. 'steps=', n printString
